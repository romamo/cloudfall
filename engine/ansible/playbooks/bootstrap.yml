---
- name: Bootstrap Cloudfall application servers
  hosts: cloudfall_servers
  become: true
  gather_facts: true
  any_errors_fatal: true
  serial: 1
  roles:
    - role: cloudfall_bootstrap
      cloudfall_bootstrap_projects: "{{ cloudfall_projects }}"
      cloudfall_bootstrap_components: "{{ cloudfall_components }}"
