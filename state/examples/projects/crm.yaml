---
apiVersion: atlas/v1
kind: Project
metadata:
  id: crm
  description: Example customer relationship management product
spec:
  linuxUser: crm
  approval: manual
  components:
    - crm-backend
  secretRefs:
    - scope: platform
      environment: production
      path: /shared
