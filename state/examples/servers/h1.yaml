---
apiVersion: cloudfall/v1
kind: Server
metadata:
  id: h1
  description: Primary application host
spec:
  hostname: h1.example.internal
  address: h1.example.internal
  environment: production
  profile: debian-application
  lifecycle: active
  ssh:
    user: cloudfall
    port: 22
  labels:
    region: eu-central
    workload: application
